# ============================================================
# HireWise - Multi-Agent Recruitment Decision Support System
# Core runtime dependencies
# ============================================================

# --- Web framework ---------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9          # file uploads
pydantic>=2.7.0
pydantic-settings>=2.3.0
sqlmodel>=0.0.22                # ORM (SQLAlchemy 2 + Pydantic) -> SQLite / PostgreSQL

# --- Database driver (PostgreSQL). SQLite is used by default for the demo. ---
psycopg2-binary>=2.9.9

# --- Information Retrieval (vector store) ----------------------------------
chromadb>=0.5.0

# --- Document / text parsing ----------------------------------------------
PyMuPDF>=1.24.0                 # PDF text extraction (import: fitz)
python-docx>=1.1.0               # DOCX extraction
spacy>=3.7.0                     # NER for PII / names
regex>=2023.12.25

# --- Security --------------------------------------------------------------
PyJWT>=2.8.0                     # JWT tokens
bcrypt>=4.1.0                    # password hashing
cryptography>=42.0.0             # Fernet encryption for stored CVs

# --- Testing / tooling -----------------------------------------------------
pytest>=8.0.0
httpx>=0.27.0

# --- Optional LLM integration (Gemini / OpenAI format) ---------------------
#   See requirements-llm.txt for the optional providers.