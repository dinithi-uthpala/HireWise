"""REST API route modules.

Planned modules:
    auth.py     login / token + RBAC-protected user endpoints
    jobs.py     create vacancy + requirement analysis
    candidates.py   upload CVs + run the multi-agent pipeline
    dashboard.py    ranking, candidate detail, comparisons
    decisions.py    human decision / override endpoints
    audit.py        audit trail + agent-activity monitor
    fairness.py     fairness test mode (paired CVs)
"""